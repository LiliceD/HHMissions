{% extends 'base.html.twig' %}


{% block nav %}
{% endblock %}


{% block body %}
  <div class="container col-md-7 col-lg-5 mt-sm-5">
    <div class="card bg-light text-center">

      <div class="card-header pt-3">
        <h4>Connexion</h4>
      </div>

      <div class="card-body">
        {% if error %}
          <div class="alert alert-danger">
            {{ error.messageKey|trans(error.messageData, 'security') }}
          </div>
        {% endif %}

        <form action="{{ path('login') }}" method="post">
          <div class="mb-4">
            <label for="username" class="col-sm-auto">Nom d'utilisateur :</label>
            <input type="text" id="username" name="_username" class="col-sm-auto form-alike" value="{{ last_username }}" autofocus="true"/>
          </div>
          
          <div class="mb-4">
            <label for="password" class="col-sm-auto">Mot de passe :</label>
            <input type="password" id="password" name="_password" class="col-sm-auto form-alike" />
          </div>

          <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" />
          
          {#
              If you want to control the URL the user
              is redirected to on success (more details below)
              <input type="hidden" name="_target_path" value="/account" />
          #}
          <div class="">
            <button type="submit" class="btn btn-primary">Valider</button>
          </div>
        </form>
      </div> {# div card-body #}

    </div> {# div card #}
  </div> {# div container #}
{% endblock %}
